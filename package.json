{
  "name": "focusflow",
  "version": "1.0.0",
  "description": "Desktop productivity tracker — active window time, classification, and focus reminders",
  "main": "src/main.js",
  "author": "FocusFlow",
  "license": "MIT",
  "scripts": {
    "start": "node scripts/start.js",
    "start:electron": "electron . --no-sandbox --disable-gpu",
    "start:demo": "FOCUSFLOW_DEMO=1 FOCUSFLOW_THRESHOLD_SEC=30 node scripts/start.js",
    "test": "node scripts/smoke.js"
  },
  "dependencies": {
    "active-win": "^8.1.1",
    "electron": "^28.3.3"
  }
}
